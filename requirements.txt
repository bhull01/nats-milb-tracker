# Washington Nationals MiLB Tracker – Python dependencies
# Install: pip install -r requirements.txt
#
# SQLite is used locally; PostgreSQL (via DATABASE_URL) in production.

requests>=2.28.0
flask>=3.0.0
psycopg2-binary>=2.9.0
gunicorn>=21.2.0
